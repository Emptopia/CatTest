using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Fish : Entity
{
    // Start is called before the first frame update
    void Start()
    {
        this.entityType = EntityType.Fish;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
